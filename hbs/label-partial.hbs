<label class="bonus-label{{#if isSubLabel}} sub-label{{/if}} {{extraClasses}}">
    {{label}}
    {{#if (or tooltip journal)}}
    <a
        data-tooltip="{{tooltip}}"
        {{#if journal}}data-journal="{{journal}}"{{/if}}
    >
        <i class="fas fa-book"></i>
    </a>
    {{/if}}
</label>
