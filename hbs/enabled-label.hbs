<div class="form-group roll-bonuses">
    <label>{{label}}</label>
    <div class="form-fields">
        {{#if subLabel}}
        <div>{{subLabel}}</div>
        {{/if}}
        <i class="label">{{localize "PF1.Enabled"}}</i>
    </div>
</div>
