<div class="form-group roll-bonuses">
    {{> "modules/ckl-roll-bonuses/hbs/label.hbs"}}
    <div class="form-fields">
        {{#if subLabel}}
        <div>{{subLabel}}</div>
        {{/if}}
        <i class="label" {{#if for}}for={{for}}{{/if}}>{{localize "PF1.Enabled"}}</i>
    </div>
</div>
